version: 2.1
executors:
  docker-publisher:
    environment:
      IMAGE_NAME: 14876231/loginmicroservices-0.0.1
    docker:
      - image: circleci/buildpack-deps:stretch
jobs:
  build:
    executor: docker-publisher
    steps:
      - checkout
      - setup_remote_docker
      - run:
          name: Build Docker image
          command: |
            docker build -t 14876231/loginmicroservices-0.0.1:latest .
      - run:
          name: Archive Docker image
          command: docker save -o image.tar 14876231/loginmicroservices-0.0.1
      - persist_to_workspace:
          root: .
          paths:
            - ./image.tar
  publish-latest:
    executor: docker-publisher
    steps:
      - attach_workspace:
          at: /tmp/workspace
      - setup_remote_docker
      - run:
          name: Load archived Docker image
          command: docker load -i /tmp/workspace/image.tar
      - run:
          name: Publish Docker Image to Docker Hub
          command: |
            echo $DOCKERHUB_PASS | docker login -u "14876231" --password-stdin
            docker push 14876231/loginmicroservices-0.0.1 :latest
workflows:
  version: 2
  build-master:
    jobs:
      - build:
          filters:
            branches:
              only: develop
      - publish-latest:
          requires:
            - build
          filters:
            branches:
              only: develop
